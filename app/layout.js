export const metadata = {
  title: "CCC 11 Field Planner",
  description: "Kraków Cricket League field placement",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
